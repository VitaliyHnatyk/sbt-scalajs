val crossversion_default = "SymLinkedBuild" // "CommonBaseBuild" "SharedBuild" "SymLinkedBuild"

val crossversion_rdf = System.setProperty("crossversion-rdf.build", crossversion_default)

val crossversion_db = System.setProperty("crossversion-db.build",  crossversion_default)

